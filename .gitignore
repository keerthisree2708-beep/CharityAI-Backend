# Dependencies
node_modules/

# Environment variables — NEVER commit these
.env
.env.local
.env.production

# Uploaded files — use cloud storage (Cloudinary) in production
uploads/*
!uploads/.gitkeep

# Logs
*.log
npm-debug.log*

# Editor
.DS_Store
